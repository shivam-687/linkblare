import React, { PropsWithChildren } from 'react'

const Toolbar = ({children}: PropsWithChildren) => {
  return (
    <div className='w-full rounded-lg border border-solid border-gray-400'>
      
    </div>
  )
}

export default Toolbar